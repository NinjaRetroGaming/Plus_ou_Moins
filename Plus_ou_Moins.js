/*COMMENTAIRE*/
srand(time(NULL));
nombreMystere = (rand() % (MAX - MIN + 1)) + MIN;

/*COMMENTAIRE*/
const int MAX = 100, MIN = 1;

/*COMMENTAIRE*/
#include <stdio.h>
#include <stdlib.h>
#include <time.h>